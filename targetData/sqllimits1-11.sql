

    set vals [list]
    sqlite3_limit db SQLITE_LIMIT_FUNCTION_ARG $::max
    for {set i 0
SELECT max([join $vals ,])


    set vals [list]
    for {set i 0
SELECT max([join $vals ,])


    db function myfunc myfunc
    set vals [list]
    for {set i 0
SELECT myfunc([join $vals ,])
